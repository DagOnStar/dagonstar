# IoT checkpointing and recovery

IoT checkpoints use schema version 1. Completed reuse requires success, an accessible working directory, every declared output, and matching SHA-256 evidence. Waiting invocations retain provider and invocation identity; providers advertising reattachment may continue them. Unknown physical actuation outcomes are recorded and never automatically replayed. Checkpoints use the shared credential redactor.
