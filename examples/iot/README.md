# IoT examples

`python3 examples/iot/observe_mock.py` runs a bounded, credential-free sensor observation. The mock provider contacts no broker or device. See [the IoT task guide](../../docs/iot_tasks.md).
